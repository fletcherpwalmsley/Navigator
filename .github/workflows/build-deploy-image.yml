name: Deployment Contatiner

on:
  workflow_run:
    workflows: [Base Contatiner"]
    types: 
      - completed
  push:
    branches:
      - main  # Trigger the workflow on push to the main branch
  pull_request:
    branches:
      - main  # Trigger the workflow on pull request to the main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          file: docker/navigator-base/Dockerfile
          push: true
          platforms: linux/amd64
          tags: navigator-deploy:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          context: .

      # - name: Build and Push Docker image
      #   if: github.ref == 'refs/heads/main'  # Build and push only on the main branch
      #   run: |
      #     docker buildx build --push --tag ghcr.io/${{ github.repository_owner }}/navigator-deploy:latest -f docker/navigator-deploy/Dockerfile .
